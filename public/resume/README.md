# Resume PDF

Drop your resume PDF at:

```
public/resume/Eshaan-Arya-Resume.pdf
```

The site references this path from `content/profile.json` (`resumeUrl`). Update
that field if you'd prefer a different filename or location.

Recommended:

- Keep the file under 1 MB
- Use a searchable (not scanned) PDF
- Match the `.pdf` extension exactly to what's referenced in `profile.json`
